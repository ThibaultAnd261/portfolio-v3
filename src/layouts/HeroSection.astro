---
import "../styles/color.css";
import profileImage from "../assets/img/me.png";
---

<section class="hero fade-in-delay-1">
    <div class="hero-image">
        <img src={profileImage.src} alt="Photo de profil" />
    </div>
    <div class="hero-content">
        <h1>Thibault Andriamasinoro</h1>
        <div class="subtitle">Ingénieur logiciel - Spécialisé en développement front-end</div>
        <p>Je transforme des problématiques complexes en interfaces fluides et intuitives. Mon objectif est d'allier la rigueur technique à une sensibilité créative pour coder des solutions qui sont aussi performantes qu'agréables à utiliser.</p>
    </div>
</section>

<style>
    .hero {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 2rem;
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem 1.5rem;
        text-align: center;
    }

    .hero-image {
        flex-shrink: 0;
    }

    .hero-image img {
        width: 180px;
        height: 180px;
        border-radius: 50%;
        border: 4px solid var(--charcoal);
        object-fit: cover;
        box-shadow: 8px 8px 0 var(--copper);
        transition: all 0.4s var(--transition-smooth);
    }

    .hero-image img:hover {
        transform: translateY(-4px) translateX(-4px);
        box-shadow: 12px 12px 0 var(--copper);
    }

    .hero-content {
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .hero-content h1 {
        font-family: 'DM Serif Display', serif;
        font-size: clamp(2rem, 5vw, 4rem);
        line-height: 1.1;
        color: var(--charcoal);
        margin-bottom: 1rem;
    }

    .subtitle {
        font-family: 'Karla', sans-serif;
        font-size: clamp(1.1rem, 2.5vw, 1.6rem);
        font-weight: 600;
        color: var(--copper);
        margin-bottom: 1rem;
    }

    .hero-content p {
        font-size: 1rem;
        max-width: 500px;
        opacity: 0.85;
        color: var(--charcoal);
        line-height: 1.6;
    }

    /* Tablet */
    @media (min-width: 768px) {
        .hero {
            padding: 3rem 2rem;
            gap: 2.5rem;
        }

        .hero-image img {
            width: 200px;
            height: 200px;
        }

        .hero-content p {
            font-size: 1.1rem;
            max-width: 550px;
        }
    }

    /* Desktop */
    @media (min-width: 1025px) {
        .hero {
            flex-direction: row;
            text-align: left;
            padding: 4rem 2rem 3rem;
            gap: 4rem;
        }

        .hero-image img {
            width: 240px;
            height: 240px;
        }

        .hero-content {
            align-items: flex-start;
        }

        .hero-content p {
            max-width: 600px;
        }
    }
</style>
